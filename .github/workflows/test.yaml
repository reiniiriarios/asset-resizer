on:
  push:
    tags:
      - "WIP_DELETE_ME"

name: Test (WIP)

jobs:
  test:
    name: Test
    runs-on: "ubuntu-latest"

    steps:
      - uses: actions/checkout@v4

      - name: Install Dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Lint
        run: npm run lint

      - name: Test
        run: num run test
